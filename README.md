# multiplication-table-system
A simple VB.NET program that generates and displays a multiplication table (up to 10×10) based on a user-entered number from 1 to 10. It checks for invalid inputs, repeats upon user request, and ends with a thank-you message when the user chooses to stop.
